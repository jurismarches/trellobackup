============
Installation
============

At the command line::

    $ easy_install trellobackup

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv trellobackup
    $ pip install trellobackup